class MessItemClass:

	def __init__(self, enumeration, name, tool, trigger, level):

		self.enumeration = enumeration

		self.name = name

		self.tool = tool

		self.trigger = trigger

		self.level = level
